## Tools

### Prettier

- Run `pnpm run format` to format your code

### ESLint

- Run `pnpm run lint` to lint your code

## Docs

- Rslint: https://rslint.rs/llms.txt

### Rslint

- Run `pnpm run lint` to lint your code

## Development server

- 编辑过程中禁止执行 `pnpm run dev`，避免占用用户正在使用的 Dev 端口 3000。
